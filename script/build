echo "copy licenses"

mkdir _site/raw -p

for file in _licenses/*.txt; do
	echo "$file"
	awk '/^---$/ {if (++count == 2) {p=1; next}} p' "$file" | tail -n +2 > "_site/raw/$(basename "$file" .txt)"
done
